export const environment = {
    production: true,
    preurl: '',
    servicios: {
        // Cambia el puerto del backend directo por la URL gestionada por NGINX
        api: 'api de produccion/',
    },
};